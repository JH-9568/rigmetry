# 목표 인터페이스를 보여주는 예시이며 실행 기능은 아직 구현되지 않았습니다.
name: basic-with-debugging

model:
  provider: ollama
  model: qwen3:8b

system_prompt: >
  변경 전 테스트를 실행하고 실패 원인을 확인한 뒤 최소한으로 수정하라.

mcps: []

tools:
  - terminal

skills:
  - ../skills/debugging/SKILL.md

runtime:
  max_steps: 20
  timeout: 120
  max_total_tokens: 50000
