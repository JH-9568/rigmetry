# 목표 인터페이스를 보여주는 예시이며 실행 기능은 아직 구현되지 않았습니다.
name: basic

model:
  provider: deepseek
  model: deepseek-chat

mcps:
  - filesystem

tools:
  - terminal

skills: []

runtime:
  max_steps: 20
  timeout: 120
  max_total_tokens: 50000
